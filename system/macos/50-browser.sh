#!/usr/bin/env bash
set -euo pipefail

echo "Default browser script not yet applied."
echo "Your capture did not confirm the current default browser."
echo "If you want Chrome forced as default, we can wire that in next."
